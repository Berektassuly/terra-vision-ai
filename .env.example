# Which AI provider the chat agent uses: "openai" or "gemini". Server-side only; no frontend toggle.
ACTIVE_AI_PROVIDER=openai

# OpenAI – required when ACTIVE_AI_PROVIDER=openai (Smart Agronomist chat).
# Create an API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini – required when ACTIVE_AI_PROVIDER=gemini.
# Create an API key at https://aistudio.google.com/apikey
GOOGLE_GENERATIVE_AI_API_KEY=

# Sentinel Hub (EU-Central-1) – required for Catalog, Process, and Statistical APIs.
# Create an OAuth client at https://apps.sentinel-hub.com/dashboard/#/account/settings
SENTINEL_CLIENT_ID=
SENTINEL_CLIENT_SECRET=
